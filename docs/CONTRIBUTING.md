# Contributing

1. Branch from `main`.
2. Edit Turtle under `ontology/`; run `./scripts/validate.sh`.
3. Open a PR with:
   - What concept you are modeling and why
   - Example usage (IRI + label)
   - Whether downstream repos need updates

Keep diffs focused — one conceptual area per PR when possible.
